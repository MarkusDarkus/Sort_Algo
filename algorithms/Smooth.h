#ifndef SMOOTH_H
#define SMOOTH_H

#include <algorithm>
#include <vector>

int leonardo(int k);
void heapify(std::vector<int>& arr, int start, int end);
std::vector<int> smooth_sort(std::vector<int>& arr);

#endif // SMOOTH_H
